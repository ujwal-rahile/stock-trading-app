server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/customerdb
    username: ujwal
    password: ujwal
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect