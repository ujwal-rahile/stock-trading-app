server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tradedb
    username: ujwal
    password: ujwal
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect